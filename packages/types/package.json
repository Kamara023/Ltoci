{
  "name": "@lotostats/types",
  "version": "0.1.0",
  "private": true,
  "description": "Types et enums partagés entre les services et les fronts LotoStats",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@lotostats/config": "workspace:*",
    "typescript": "^5.7.0"
  }
}
